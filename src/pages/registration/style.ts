import { styled } from 'styled-components';

export const StyledPageHeadingWrapper = styled.div`
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 20px;
`;

export const StyledPageContentWrapper = styled.div`
  display: flex;
  flex-direction: column;
  gap: 50px;

  margin: 80px 25px;
`;
