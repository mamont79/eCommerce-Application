import styled from 'styled-components';

export const StyledEmptyCartWrapper = styled.div`
  max-width: 700px;
  min-width: 300px;
  margin: 0 auto;
  font-size: 1.2rem;
  display: flex;
  flex-direction: column;
  justify-content: space-around;
`;

export const StyledEmptyCartTitle = styled.h2`
  text-align: center;
  text-transform: uppercase;
`;

export const StyledMessageEmptyCart = styled.p`
  text-align: center;
  font-size: 1.5rem;
`;

export const StyledNotEmptyCart = styled.h2`
  text-align: center;
  text-transform: uppercase;
`;
