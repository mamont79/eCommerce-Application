import styled from 'styled-components';

const StyledWraperHeader = styled.header`
  display: flex;
  vertical-align: middle;
  width: 100%;
  height: 83px;
  background: #ffe978;
`;

export default StyledWraperHeader;
